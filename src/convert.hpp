#include <iostream>

std::string textToUrl(std::string input);
std::string urlToText(std::string input);

std::string contentToBase64(std::string input);
std::string base64ToContent(std::string input);
