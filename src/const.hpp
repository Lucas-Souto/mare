#include <iostream>
#ifndef CONSTS_HPP
#define CONSTS_HPP
static const int DEFAULT_PORT = 8000;
static const int BUFFER_SIZE = 30720;
static const int MAX_DIRECTORIES = 10;

std::string statusText(int status);
#endif
